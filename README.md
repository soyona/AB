# AB

AB is an SAPDP-compatible product workspace.

Current lifecycle state is defined by `PROJECT_STATE.md`.

The frozen Problem Definition is recorded at
`docs/problem/ProblemDefinition_CORE_v1.md`.

The active stage is Solution. The next action is to create
`SolutionDefinition_CORE_v1.md` from
`templates/solution/SolutionDefinition_Template.md`. Entry into Solution was
authorized by explicit Human override without the SAPDP v6.0.0 Problem
Execution Closure Artifact.
