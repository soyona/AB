# AB

AB is an SAPDP-compatible product workspace.

Current lifecycle state is defined by `PROJECT_STATE.md`.

The frozen Problem Definition is recorded at
`docs/problem/ProblemDefinition_CORE_v1.md`. Transition to Solution is blocked
until the SAPDP execution-governance authority resolves a registered
product-development governance profile and Runtime Findings Registry path.
