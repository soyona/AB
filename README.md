# AB

AB is an SAPDP-compatible product workspace.

Current lifecycle state is defined by `PROJECT_STATE.md`. Bootstrap stops before
product lifecycle execution; the next action is to create
`ProblemDefinition_CORE_v1.md` from
`templates/problem/ProblemDefinition_Template.md`.
